'use client';
import { Activity, AlertCircle, ArrowUpRight, CalendarClock, Check, Clock3, FileBarChart, Lock, UserRoundCheck } from 'lucide-react'; import { Bar, BarChart, Cell, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts'; import { Alert, Badge, Skeleton } from '../../components/ui/feedback'; import { areaChart, kpis, stages, statusChart } from './fixtures';
const tones: Record<string, string> = { action: 'border-action', success: 'border-success', academic: 'border-academic', danger: 'border-danger', primary: 'border-primary', warning: 'border-warning' };
export function KpiCard({ label, value, tone }: { label: string; value: string; tone: string }) { return <article className={`rounded-xl border border-border border-l-4 bg-white p-4 shadow-sm ${tones[tone] ?? 'border-action'}`}><p className="text-sm font-medium text-slate-600">{label}</p><p className="mt-2 text-3xl font-bold tracking-tight text-primary">{value}</p><p className="mt-2 text-xs text-slate-500">Fixture de demonstração</p></article>; }
export function EventProgress() { return <section className="rounded-xl border border-border bg-white p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-primary">Progresso do evento</h2><p className="mt-1 text-sm text-slate-500">Avaliação em andamento</p></div><Badge tone="academic">2 de 9 etapas</Badge></div><ol className="mt-6 flex min-w-max items-start">{stages.map((stage, i) => <li key={stage.label} className="flex w-28 flex-col items-center text-center"><div className="flex w-full items-center"><span className={`relative z-10 grid size-8 shrink-0 place-items-center rounded-full border-2 ${stage.status === 'done' ? 'border-success bg-success text-white' : stage.status === 'current' ? 'border-action bg-white text-action ring-4 ring-blue-100' : 'border-slate-300 bg-slate-100 text-slate-400'}`}>{stage.status === 'done' ? <Check className="size-4"/> : stage.status === 'future' ? <Lock className="size-3"/> : i + 1}</span>{i < stages.length - 1 && <span className={`h-0.5 flex-1 ${stage.status === 'done' ? 'bg-success' : 'bg-slate-200'}`} />}</div><span className={`mt-2 text-xs ${stage.status === 'current' ? 'font-bold text-primary' : 'text-slate-600'}`}>{stage.label}</span><span className="mt-1 text-[10px] text-slate-500">{stage.status === 'done' ? 'Concluída' : stage.status === 'current' ? 'Em andamento' : 'Futura'}</span></li>)}</ol></section>; }
function ChartCard({ title, children }: { title: string; children: React.ReactNode }) { return <section className="rounded-xl border border-border bg-white p-5"><h2 className="font-semibold text-primary">{title}</h2><div className="mt-4 h-64">{children}</div></section>; }
export function Charts() { return <div className="grid gap-4 xl:grid-cols-2"><ChartCard title="Trabalhos por status"><ResponsiveContainer><PieChart><Pie data={statusChart} dataKey="value" nameKey="name" innerRadius={55} outerRadius={85} paddingAngle={3}>{statusChart.map((item) => <Cell key={item.name} fill={item.fill}/>)}</Pie><Tooltip /></PieChart></ResponsiveContainer></ChartCard><ChartCard title="Trabalhos por área"><ResponsiveContainer><BarChart data={areaChart}><XAxis dataKey="name" tick={{ fontSize: 11 }} interval={0}/><YAxis allowDecimals={false}/><Tooltip/><Bar dataKey="value" fill="#0061A5" radius={[4,4,0,0]} /></BarChart></ResponsiveContainer></ChartCard></div>; }
export function Operations() { const deadlines = [['15 Out', 'Fechamento de notas', 'Data limite para avaliadores inserirem as notas.'], ['20 Out', 'Divulgação de aprovados', 'Publicação da lista preliminar.'], ['05 Nov', 'Cerimônia de premiação', 'Evento presencial no auditório principal.']]; return <div className="grid gap-4 xl:grid-cols-3"><section className="rounded-xl border border-border bg-white p-5 xl:col-span-1"><div className="flex items-center justify-between"><h2 className="font-semibold text-primary">Próximos prazos</h2><CalendarClock className="size-5 text-action"/></div><div className="mt-4 space-y-4">{deadlines.map(([date,title,description]) => <div className="flex gap-3" key={title}><span className="grid size-11 shrink-0 place-items-center rounded-lg bg-slate-100 text-center text-xs font-bold text-primary">{date}</span><div><h3 className="text-sm font-semibold">{title}</h3><p className="mt-0.5 text-xs text-slate-500">{description}</p></div></div>)}</div></section><section className="rounded-xl border border-border bg-white p-5 xl:col-span-1"><div className="flex items-center justify-between"><h2 className="font-semibold text-primary">Avaliações</h2><FileBarChart className="size-5 text-academic"/></div><div className="mt-5 grid grid-cols-3 gap-3 text-center"><div><p className="text-2xl font-bold text-success">134</p><p className="text-xs text-slate-500">Concluídas</p></div><div><p className="text-2xl font-bold text-academic">62</p><p className="text-xs text-slate-500">Pendentes</p></div><div><p className="text-2xl font-bold text-danger">8</p><p className="text-xs text-slate-500">Em atraso</p></div></div><div className="mt-6 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full w-[66%] bg-success"/></div><p className="mt-2 text-xs text-slate-500">66% concluídas</p></section><section className="rounded-xl border border-border bg-white p-5 xl:col-span-1"><div className="flex items-center justify-between"><h2 className="font-semibold text-primary">Atividade recente</h2><Activity className="size-5 text-action"/></div><ul className="mt-4 space-y-3 text-sm text-slate-600"><li>Mariana Costa importou 32 trabalhos.</li><li>Carlos Lima concluiu uma avaliação.</li><li>Ana Beatriz alterou o cadastro CON-2026-0042.</li><li>Coordenação atualizou o prazo de avaliação.</li></ul></section></div>; }
export function DashboardLoading() { return <div className="grid grid-cols-1 gap-4 sm:grid-cols-2 xl:grid-cols-3">{Array.from({ length: 6 }, (_, index) => <Skeleton key={index} className="h-32" />)}</div>; }
export function OperationalAlert() { return <Alert title="Atenção necessária"><ul className="grid gap-1 sm:grid-cols-3"><li>12 trabalhos sem avaliador</li><li>8 avaliações próximas do prazo</li><li>5 cadastros incompletos</li></ul></Alert>; }
