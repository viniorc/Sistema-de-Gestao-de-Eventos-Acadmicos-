'use client'; import Link from 'next/link'; import { ArrowLeft, MailCheck } from 'lucide-react'; import { useState } from 'react'; import { api } from '../../lib/api'; import { Button } from '../../components/ui/button'; import { Input } from '../../components/ui/form-fields';
export default function RecoverAccessPage() { const [email, setEmail] = useState(''); const [sent, setSent] = useState(false); const [loading, setLoading] = useState(false); const submit = async (event: React.FormEvent) => { event.preventDefault(); setLoading(true); try { await api.forgotPassword(email); } finally { setLoading(false); setSent(true); } }; return <main className="grid min-h-screen place-items-center bg-background p-5"><div className="grid w-full max-w-4xl overflow-hidden rounded-xl border border-border bg-white shadow-xl md:grid-cols-5"><section className="hidden bg-primary p-8 text-white md:col-span-2 md:block"><p className="text-3xl font-bold">CONEXÃO</p><p className="mt-2 text-sm text-blue-200">Gestão de Eventos Acadêmicos</p><p className="mt-1 text-xs text-blue-300">PROMIC</p></section><section className="p-7 sm:p-10 md:col-span-3"><div className="mb-6 grid size-12 place-items-center rounded-full bg-blue-50 text-action"><MailCheck className="size-6"/></div><h1 className="text-2xl font-bold text-primary">Recuperar acesso</h1><p className="mt-2 text-slate-600">Informe seu e-mail institucional para receber as instruções de recuperação.</p>{sent ? <div className="mt-7 rounded-xl border border-border bg-slate-50 p-5"><p className="font-semibold text-primary">E-mail enviado</p><p className="mt-1 text-sm text-slate-600">Se o endereço estiver cadastrado, enviaremos as instruções de recuperação.</p></div> : <form onSubmit={submit} className="mt-7 space-y-5"><Input label="E-mail institucional" type="email" required placeholder="nome@instituicao.edu.br" value={email} onChange={(event) => setEmail(event.target.value)}/><Button className="w-full" loading={loading}>Enviar instruções</Button></form>}<Link className="mt-7 inline-flex items-center gap-2 text-sm font-semibold text-action hover:underline" href="/login"><ArrowLeft className="size-4"/>Voltar para o login</Link></section></div></main>; }
