import { LoaderCircle, type LucideIcon } from 'lucide-react'; import { type ButtonHTMLAttributes } from 'react'; import { cn } from '../../lib/utils';
type Variant = 'primary' | 'secondary' | 'ghost' | 'destructive' | 'icon';
export function Button({ className, variant = 'primary', loading, children, ...props }: ButtonHTMLAttributes<HTMLButtonElement> & { variant?: Variant; loading?: boolean }) { const styles: Record<Variant, string> = { primary: 'bg-action text-white hover:bg-action-hover', secondary: 'bg-primary text-white hover:bg-slate-900', ghost: 'text-action hover:bg-blue-50', destructive: 'bg-danger text-white hover:bg-red-800', icon: 'text-slate-600 hover:bg-slate-100' }; return <button className={cn('inline-flex min-h-10 items-center justify-center gap-2 rounded-lg px-4 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50', styles[variant], variant === 'icon' && 'w-10 p-0', className)} disabled={loading || props.disabled} {...props}>{loading && <LoaderCircle className="size-4 animate-spin" aria-hidden />}{children}</button>; }
export function IconButton({ icon: Icon, label, ...props }: { icon: LucideIcon; label: string } & Omit<React.ComponentProps<typeof Button>, 'children' | 'variant'>) { return <Button variant="icon" aria-label={label} title={label} {...props}><Icon className="size-5" /></Button>; }
