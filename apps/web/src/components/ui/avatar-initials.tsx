import { cn } from '../../lib/utils'; export function AvatarInitials({ name = 'Mariana Costa', className }: { name?: string; className?: string }) { const initials = name.split(' ').map((part) => part[0]).slice(0, 2).join(''); return <span aria-label={name} className={cn('inline-flex size-9 items-center justify-center rounded-full bg-primary text-xs font-bold text-white', className)}>{initials}</span>; }
