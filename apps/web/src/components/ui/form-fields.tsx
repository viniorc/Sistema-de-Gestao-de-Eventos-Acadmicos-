'use client'; import { Eye, EyeOff } from 'lucide-react'; import { useState } from 'react'; import { cn } from '../../lib/utils';
export const inputClass = 'min-h-11 w-full rounded-lg border border-border bg-white px-3 text-sm text-slate-900 placeholder:text-slate-400 focus:border-action focus:ring-2 focus:ring-blue-100';
export function Input({ label, error, className, ...props }: React.InputHTMLAttributes<HTMLInputElement> & { label?: string; error?: string }) { const id = props.id ?? props.name; return <div className="space-y-1.5">{label && <label htmlFor={id} className="text-sm font-semibold text-slate-700">{label}</label>}<input id={id} className={cn(inputClass, error && 'border-danger', className)} {...props}/>{error && <p className="text-xs text-danger">{error}</p>}</div>; }
export function PasswordInput(props: Omit<React.InputHTMLAttributes<HTMLInputElement>, 'type'> & { label?: string; error?: string }) { const [shown, setShown] = useState(false); const id = props.id ?? props.name; return <div className="space-y-1.5"><label htmlFor={id} className="text-sm font-semibold text-slate-700">{props.label ?? 'Senha'}</label><div className="relative"><input id={id} className={cn(inputClass, 'pr-11', props.error && 'border-danger')} type={shown ? 'text' : 'password'} {...props}/><button className="absolute inset-y-0 right-0 px-3 text-slate-500" type="button" aria-label={shown ? 'Ocultar senha' : 'Mostrar senha'} onClick={() => setShown(!shown)}>{shown ? <EyeOff className="size-5"/> : <Eye className="size-5"/>}</button></div>{props.error && <p className="text-xs text-danger">{props.error}</p>}</div>; }
