import { AlertTriangle } from 'lucide-react'; import { cn } from '../../lib/utils';
export function Badge({ children, tone = 'neutral' }: { children: React.ReactNode; tone?: 'neutral' | 'success' | 'warning' | 'danger' | 'academic' }) { const styles = { neutral: 'bg-slate-100 text-slate-700', success: 'bg-emerald-50 text-success', warning: 'bg-amber-50 text-warning', danger: 'bg-red-50 text-danger', academic: 'bg-violet-50 text-academic' }; return <span className={cn('inline-flex rounded-full px-2.5 py-1 text-xs font-semibold', styles[tone])}>{children}</span>; }
export function Alert({ title, children, tone = 'warning' }: { title: string; children: React.ReactNode; tone?: 'warning' | 'danger' }) { return <section className={cn('rounded-xl border p-4', tone === 'warning' ? 'border-amber-200 bg-amber-50' : 'border-red-200 bg-red-50')}><div className="flex gap-3"><AlertTriangle className={cn('mt-0.5 size-5 shrink-0', tone === 'warning' ? 'text-warning' : 'text-danger')} /><div><h2 className="font-semibold">{title}</h2><div className="mt-1 text-sm text-slate-700">{children}</div></div></div></section>; }
export function Skeleton({ className }: { className?: string }) { return <div className={cn('animate-pulse rounded bg-slate-200', className)} />; }
export function EmptyState({ title, description, action }: { title: string; description: string; action?: React.ReactNode }) { return <div className="rounded-xl border border-dashed border-border bg-white px-6 py-12 text-center"><h2 className="font-semibold text-primary">{title}</h2><p className="mx-auto mt-2 max-w-sm text-sm text-slate-600">{description}</p>{action && <div className="mt-4">{action}</div>}</div>; }
