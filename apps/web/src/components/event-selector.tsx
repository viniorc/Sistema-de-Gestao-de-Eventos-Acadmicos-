'use client'; import { ChevronDown } from 'lucide-react'; import { useState } from 'react'; import { useSelectedEvent } from '../features/events/event-provider'; import { Badge } from './ui/feedback';
export function EventSelector() { const { events, selectedEvent, isLoading, selectEvent } = useSelectedEvent(); const [open, setOpen] = useState(false); if (isLoading) return <div className="h-10 w-52 animate-pulse rounded-lg bg-slate-100"/>; if (!selectedEvent) return null; return <div className="relative"><button onClick={() => setOpen(!open)} className="flex min-h-10 items-center gap-2 rounded-lg border border-border bg-white px-3 text-sm font-semibold text-primary" aria-expanded={open}><span>{selectedEvent.name} {selectedEvent.year}</span><Badge tone={selectedEvent.status === 'ACTIVE' ? 'success' : 'neutral'}>{selectedEvent.status === 'ACTIVE' ? 'Ativo' : 'Encerrado'}</Badge><ChevronDown className="size-4" /></button>{open && <div className="absolute z-50 mt-2 w-72 rounded-xl border border-border bg-white p-1 shadow-lg">{events.map((event) => <button key={event.id} onClick={() => { selectEvent(event); setOpen(false); }} className="flex w-full items-center justify-between rounded-lg px-3 py-2.5 text-left text-sm hover:bg-slate-50"><span>{event.name} {event.year}</span><Badge tone={event.status === 'ACTIVE' ? 'success' : 'neutral'}>{event.status === 'ACTIVE' ? 'Ativo' : 'Encerrado'}</Badge></button>)}</div>}</div>; }
